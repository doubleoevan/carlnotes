## 1. Correct the two sentences

- [x] 1.1 Rewrite the `domain-schema` invite paragraph so the topic-invite and team-invite clauses
      each name invite acceptance and token acceptance, the two paths the same paragraph already lists
- [x] 1.2 Rewrite the `invitations` username paragraph into a sentence with a working verb, naming the
      invite form as the thing that shows a chip and reports an unknown username
- [x] 1.3 Say reject rather than refuse in the sentences being rewritten, matching the invite
      vocabulary elsewhere. The wider `refus*` sweep across the other specs is not part of this change

## 2. Verify

- [x] 2.1 `openspec validate fix-broken-spec-sentences` passes
- [x] 2.2 Neither "acceptance or acceptance" nor "surfaces stage" survives anywhere under `openspec/`
