## 1. Schema: invites and the manual-scan marker

- [x] 1.1 Add `topic_invites` to `db/schema.ts`: `topic_id` → `topics.id` (cascade), `email`, `invited_at`, with a composite primary key `(topic_id, email)` so re-inviting is a no-op.
- [x] 1.2 Add `is_manual` to `scans`: boolean, not null, default false.
- [x] 1.3 Generate the migration with `bun run db:generate` (`0014_cooing_falcon.sql`); confirmed it only adds the table and the column.
- [x] 1.4 Extend `db/schema.test.ts`: the invite composite key + cascade, and the `is_manual` default.
- [x] 1.5 Update `db/seed.ts`: `top_agent_infra` is now an invite topic with `maya@example.com` and `theo@example.com` invited, upserting visibility so re-seeding applies it.
- [x] 1.6 Applied the migration and re-seeded the dev branch (seed run twice, idempotent).

## 2. Shared contracts

- [x] 2.1 Add `editableSourceKinds` (rss, reddit, youtube, search) to `shared/enums.ts`.
- [x] 2.2 Add to `shared/contracts.ts`: `topicScan`, `topicPageResponse`, `updateTopicPayload` (kept sources travel id-only so non-editable kinds survive a save), `subscriptionPayload`, and `manualScanResponse`.

## 3. Worker seams

- [x] 3.1 `runTopicScan` accepts an `isManual` flag recorded on the Scan row (default false, existing callers untouched).
- [x] 3.2 Add `attachmentStream` to `worker/storage.ts`; re-export the attachment/storage seams from `worker/index.ts`.

## 4. Topic page API

- [x] 4.1 Extract `loadTopicFindings` from `api/topicFeed.ts` and export `hasSubscription`.
- [x] 4.2 Add `api/topicPage.ts` with the loaders, writers, quota, and attachment helpers, plus the pure `toSourceSummary` and `startOfUtcDay`.
- [x] 4.3 Wire the routes in `api/index.ts` with zod validation, including the 429 quota rejection and the streamed download.
- [x] 4.4 Add the `api → worker` tsconfig reference (in-process bridge until Temporal lands, noted in design.md).
- [x] 4.5 Add `api/topicPage.test.ts` covering the pure helpers.

## 5. Topic page UI

- [x] 5.1 Add the `dialog` primitive built on the radix-ui Dialog.
- [x] 5.2 Give `TopicResource` an optional handlers prop; the detail page reloads its own payload after open/consume/rate.
- [x] 5.3 Add the topic page client functions to `ui/src/lib/topicClient.ts` (upload uses plain multipart fetch, everything else the typed client).
- [x] 5.4 Rebuild `ui/src/pages/TopicPage.tsx` per the spec, including the loading skeleton and the not-found state.
- [x] 5.5 Add `ui/src/components/topic-page/EditTopicModal.tsx` with save-time application and an inline error line.
- [x] 5.6 Add the delete-confirmation dialog with the spec copy; delete refreshes the homepage feed and navigates home.
- [x] 5.7 Remove the `/topics/:id/edit` route and delete `ui/src/pages/EditTopicPage.tsx`.

## 6. Verification

- [x] 6.1 `bunx biome check .` (exit 0), `bunx tsc -b`, `bun test` (51 pass) all green.
- [x] 6.2 Verified in the browser against seeded dev data: the detail page matches the mock (top row, title + "3 new", tags + ✎ 🗑 🔔, findings capped at 5 with a working "+ 2 more / show less" expander, history, and the info card with source summaries and ✉ invite); the edit modal round-tripped a rename and back (sources and invitees survived reconciliation); the invitee editor showed for invite visibility with the seeded emails; Run now decremented the quota to "2 left today" and its Scan appeared as running, then failed honestly (no LiteLLM proxy locally, and the live feed had a malformed entity — per-source failure isolation held); the delete dialog showed the live-count copy and Keep it dismissed it; the bell toggled Subscribe → Unsubscribe → back with persistence; zero console errors.
- [x] 6.3 `openspec validate add-topic-detail-and-edit-pages --strict` → valid.

## 7. Review follow-ups

- [x] 7.1 The homepage topic title links to `/topics/:id`.
- [x] 7.2 The ✎ 🗑 🔔 glyphs get hover/focus tooltips matching the theme toggle's.
- [x] 7.3 Sources display the default web scout (the search Source, on/off) in addition to the custom sources, on the info card and in the edit modal; the scout is toggleable from the modal and the add picker narrows to rss/reddit/youtube. The server summary for search stays empty because its adapter ignores the config.
- [x] 7.4 The topic page carries the shared All/Unread toggle in a homepage-style control row (toggle left, Run-now right) and its findings honor the toggle plus the resource-kind filters.
- [x] 7.5 The info card widens to a 300px rail with more breathing room (gap-x-8, p-5).
- [x] 7.6 Scan cost is admin-only: `users.role` lands (text, default `user`, migration `0013`; the seed makes the dev user an admin) and the api nulls `cost` for non-admins, so the popover appends the cents only when the payload carries it.
- [x] 7.7 The edit modal takes over the dialog's auto focus so the Title input is focused without radix selecting its text.
- [x] 7.8 The quota line "N left today" is itself the pricing link, with an "Upgrade for more" tooltip replacing the separate upgrade line.
- [x] 7.9 The control row moves above `← All topics` so the toggle and Run-now align with the homepage's toggle and Refresh.
- [x] 7.10 The header, findings, and history sections play the homepage's staggered hydrate entrance (a filter change replays the findings section), and the skeleton mirrors the full layout: control row, back link, header, findings rows, and the history-plus-card grid.
- [x] 7.11 The skeleton matches the real proportions: a title bar at the display size with the "N new" placeholder, badge-height tag pills with the three action glyphs, chevroned section headers, an expander line, and a bordered info card with per-section label and text bars.
- [x] 7.12 The tags "+" opens a github-style picker: a filter input over tags known from the loaded feed, click-to-add suggestion rows, and a Create row (Enter creates the typed tag or takes the top suggestion; the picker stays open for adding several).
- [x] 7.13 The control row and back link render statically before the payload (Run now disabled while loading, hidden for non-owners after load) and only the quota line hydrates in once known.
- [x] 7.14 The homepage's ambient steam rings sit behind the topic page.
- [x] 7.15 The Run-now triangle is pinned to its outline form (`fill-none`) in every state.
- [x] 7.16 Failed manual scans no longer consume quota: the daily count excludes failed status, so only running and succeeded manual scans charge a slot.
- [x] 7.17 The loading run-now block shows a same-height quota-line placeholder so the page never shifts vertically when the count lands, and the homepage section counts read "N topics".
- [x] 7.18 The ambient steam moved from the pages into the Layout around the Outlet, so one instance persists across route changes and the animation runs continuously (verified: the same DOM node survives client-side navigation).
- [x] 7.19 The steam backdrop's top edge is pinned to the hero seam (flow-root wrapper around the search bar and Outlet), so rings clip under the hero instead of leaving a bare strip below it, and the info rail widened from 300px to 340px (skeleton grid matched).
- [x] 7.20 Route changes scroll back to the top (a ScrollToTop effect in the Layout), so opening a topic from low on the homepage no longer lands mid-page.
- [x] 7.21 The steam rings position against a fixed 200vh canvas instead of the page, so a route change that alters the page height no longer shifts them mid-animation (verified: identical ring anchors on both routes).
- [x] 7.22 The default source's display label is `web` (was "web scout") on the info card and in the edit modal, whose toggle now reads "+ turn on the web". The visibility select's envelope glyph carries the emoji variation selector so it sizes like the globe and lock.

## 8. Homepage creation and tag filtering

- [x] 8.1 `POST /api/topics` creates a topic with its invitees and sources from the shared payload, limited to 5 per user per UTC day (`topicsRemainingToday` rides the feed response so the homepage renders the quota without a second request).
- [x] 8.2 The topic modal doubles as create mode: no page prop means empty fields, private daily defaults, the web source on, an `Add topic` title, and save posts the create then uploads staged attachments against the new id.
- [x] 8.3 The homepage control row gains the Add Topic primary button beside Refresh with the quota line (pricing link + "Upgrade for more" tooltip, loading placeholder, disabled at zero); a successful create refreshes the feed and navigates to the new topic.
- [x] 8.4 The tag widget extracted to a shared `TagPicker` (with `RemovablePill`) carrying a `canCreate` switch; the modal uses it with creation, the homepage's new tag-filter row uses it without. Selected tags narrow sections to topics carrying any of them, and the provider owns `tagFilters` plus the feed-derived `knownTags`.
- [x] 8.5 A match-mode dropdown (Any default / All / None) sits far right in the tags row, deciding whether selected tags match topics carrying at least one, every one, or none of them.
- [x] 8.6 The tags row lost its transparency: removable pills are solid secondary like topic tags and the "+" circle carries a secondary fill.
- [x] 8.7 The row reads "Tags:" + pills + "+" on the left — the "Tags:" label link-styled and opening the picker like the "+" — and a Tag Filters button (the Filters control's icon and look) on the right opening a checked menu of the match modes, replacing the select dropdown.
- [x] 8.8 The picker's search box carries a magnifying glass, capitalized "Filter tags…" / "Filter or create tags…" placeholders, and a clear ✕ while a query is typed (shared by the homepage filter and the modal). The match modes read Any Match, All Match, and Exclude Tags — "Exclude" over "Ignore", which reads like the filter is off.
- [x] 8.9 The topic cap counts held topics, not daily creations: `topicsRemaining` (was `topicsRemainingToday`) is `TOPIC_LIMIT` minus owned topics, the Add Topic line reads "N left", and deleting a topic frees a slot. The web source summary reads "search using Carl's prompt".
- [x] 8.10 The persona attribution for Jake Van Clief (LinkedIn, Eduba, YouTube, Clief Notes) lives in a README Attribution section and a footer Attribution component with external links, left-aligned in the footer.
- [x] 8.11 The hero puts Carl (ui/public/carl-hero.png, renamed from carl-transparent.png, later carl-mascot.png) bottom-left beside the left-aligned expanded pitch copy — which also replaces the README's opening alongside the image.
- [x] 8.12 Carl now tucks visibly behind the search bar card (measured: the mascot png carries ~5% baked-in transparent padding below the art, so the -mb-4 added past the matching pb-6 pulls the real artwork, not just the image box, behind the overlap) and the copy block is tightened (mt-8→mt-6, mt-4→mt-3, pb-16→pb-8); "Carl stays up. You stay informed." now shares text-primary with "All of it." (verified matching computed color in both themes).
- [x] 8.13 Carl and the copy sit closer together (gap-6→gap-4) and the copy's trailing pb-8→pb-6 brings its last line to a measured zero-gap touch against the search bar, matching how snugly Carl already sits against it (his own overlap is invariant to the copy's padding, since both he and the search bar shift by the same amount when the row's height changes — confirmed unchanged at a 40px overlap before and after).
- [x] 8.14 The hydrate entrance moved off Carl (now static, animationName: none) onto just the copy column; the CarlNotes wordmark animates in too while the coffee-mug icon beside it stays static — verified via computed animationName on each element. The hero file is renamed to ui/public/carl-hero.png.
- [x] 8.15 The CTA line reads "Give Carl three topics. You know the ones. Carl will brew a hot cup of what you missed." in the hero and the README (re-verified the search-bar zero-gap and Carl's 40px overlap both survived the taller, three-line wrapped paragraph unchanged).
- [x] 8.16 Carl shifted 1rem right (ml-4, carrying the copy right with him so their gap-4 stays 1rem) and the copy's trailing space grew to pb-10 so its gap to the search bar also measures 1rem — the copy's left and bottom margins now match exactly (verified 16px/16px at two breakpoints; Carl's 40px overlap held unchanged, confirming the horizontal shift didn't touch the vertical mechanic).
- [x] 8.17 "Carl stays up. You stay informed." is bold (verified computed font-weight 700; the equal 16px left/bottom margins around the copy held unchanged).
- [x] 8.18 Attachment ingestion errors no longer leak infra detail to the client: a new `AttachmentValidationError` (worker/attach.ts) marks the errors that genuinely describe the user's own input (oversized file, unsupported type, bad URL, empty fetch) as safe to forward verbatim; anything else (e.g. a misconfigured LiteLLM proxy) is logged server-side and returns a generic "Carl couldn't process that attachment" message instead, over 502. Strengthened the four attach.test.ts cases that exercise these paths to assert the error class, not just the message text.
- [x] 8.19 The topic modal requires Title and Carl's Prompt: both FieldLabels carry a required asterisk and Save stays disabled until both are non-empty (verified live: clearing either independently disables Save; both are needed since the pipeline scores against the prompt and the default web source describes itself as searching "using Carl's prompt").
- [x] 8.20 Attachments can be added by URL: `POST /topics/:id/attachments/url` (new `attachmentUrlPayload`, reuses `ingestUrlAttachment`'s own validation and the multipart route's `AttachmentValidationError` split) plus a `sendAttachmentUrl` client call and a "+ add by url" reveal beside "+ upload" in the modal, staged and ingested the same way as file uploads on Save (verified live: the staged pill posts to the new route on Save, and a LiteLLM-proxy failure surfaces the same sanitized "Carl couldn't process that link" message instead of an infra leak).
- [x] 8.21 The hero and README copy read "he has notes for you" again (reverted from "updates").
- [x] 8.22 The ⓘ info-popover trigger (homepage `Topic.tsx`, `TopicResource.tsx`, and the topic page's scan popover) swapped from lucide's `Info` to `NotebookPen`, filled and in the primary accent color instead of muted gray, so the notes glyph stands out as the app's core value prop (verified in both themes).
- [x] 8.23 `TopicInfoCard` gained a "Carl's Notes" section (the latest scan's summary) above "Carl's Prompt", shown once a scan has succeeded — mirrors the homepage popover's existing "Carl's notes" block (verified live against seeded data).
- [x] 8.24 The footer gained two rows below Attribution, each `justify-between`: a copyright/tagline row ("© 2026 CarlNotes" / "MIT licensed." / "Take the code. Leave the raccoon.") and a link row (Source Code, License → github; Privacy, Terms → the new stub pages).
- [x] 8.25 New `PrivacyPage`/`TermsPage` stubs routed at `/privacy` and `/terms` inside the existing Layout, linked from the footer.
- [x] 8.26 The Edit/Delete/Subscribe icon row moved from the tags row up to share the `← All topics` line, right-aligned (extracted into a `HeaderActions` component to keep `TopicPage`'s cognitive complexity under the lint cap). The subscribe bell no longer shows for the topic's own owner, enforced both in the UI condition and in `setTopicSubscription`'s server-side check.
- [x] 8.27 `bunx biome check .` (exit 0), `bunx tsc -b` (clean), `bun test` (53 pass) all green after 8.20–8.26; verified live in the browser (light and dark).
- [x] 8.28 Polish round: the info glyph swapped again from `NotebookPen` to `SquarePen`, unfilled but bold (`strokeWidth={2.5}`, no `fill-current`, per feedback that filled read as too heavy); the hero headline stays static while only the body copy below it plays the hydrate entrance; the footer's copyright and link rows switched from edge-to-edge `justify-between` to centered with a `gap-x-6` between items; and the attachment url form's "+ add by url" (now "+ add url") reveal gained an explicit Cancel button beside Add, always on its own line below "+ upload".
- [x] 8.29 Admins bypass both quotas so they can test freely: `topicsRemaining` and the manual-scan quota (`runManualScan`, `loadTopicPage`'s `manualScansRemaining`) report a fixed `ADMIN_QUOTA` (999) for an admin instead of the real count, and `runManualScan` skips the daily-limit check entirely for them. Verified live: a topic and a manual scan previously blocked at 0 remaining both went through immediately after the change landed, with the UI showing "999 left" and Run Now/Add Topic enabled.
- [x] 8.30 The stubbed free tier's real numbers: 3 topics (`TOPIC_LIMIT`, was 5) and 5 scans per day (`DAILY_SCAN_LIMIT`, renamed from `MANUAL_SCAN_DAILY_LIMIT`, was 3 manual-only). The daily count is no longer manual-only — `manualScansToday` (renamed `scansToday`) dropped its `isManual` filter, so scheduled and manual scans now share one pool with no way to tell them apart against the cap. No schema change: `scans.isManual` stays for bookkeeping, it just no longer gates the quota.
- [x] 8.31 The hardcoded quota constants moved to a billing plan: `shared/enums.ts` gained `plans` (free/plus/pro), a new `shared/planLimits.ts` exports `PLAN_LIMITS` (`as const satisfies Record<Plan, {topicLimit, dailyScanLimit}>` — free 3/5, plus 10/20, pro 50/100) and the derived `Plan` type. `db/schema.ts` adds a `plan` pgEnum and `users.plan` column (default `free`), migration `0014_glamorous_hardball.sql`; `db/schema.test.ts` pins the enum values and the column's non-null default, mirroring the existing `users.role` test. `api/topicPage.ts`'s `isAdminUser` became `userAccess` (reads `role` and `plan` in one row), and `topicsRemaining`/`runManualScan`/`remainingScans` now look up the caller's own plan's limits instead of a single global constant — admins still bypass both via the existing `ADMIN_QUOTA` path. Fixed the `scan-history` spec, which had gone stale after 8.30 (still said "3 per user per day" and "manual marker so scheduled Scans never consume quota" — now describes the plan-based, shared-pool behavior that's actually shipped). No seed changes: `users.plan` defaults to `free` for every seeded user including the dev/admin user, for whom the plan is moot since admin bypass wins regardless.
- [x] 8.32 `bunx biome check .`, `bunx tsc -b`, `bun test` (54 pass) all green after 8.31.
- [x] 8.33 `shared/planLimits.ts` renamed `shared/plans.ts` (`git mv`), its export renamed `PLAN_LIMITS` → `PLANS` since it now holds pricing alongside the quota limits, not just limits. Added `priceMonthlyCents`/`priceYearlyCents` per plan (cents, not dollars, to avoid float rounding): free $0, plus $9/mo or $90/yr, pro $29/mo or $290/yr — yearly is a flat 10x monthly on both paid plans, i.e. two months free. The `satisfies Record<Plan, {...}>` shape grew the same way, so a plan missing a price is still a compile error. No pricing page or checkout exists yet — this is the config a future one would read from, per the user's question about where pricing should live.
- [x] 8.34 The yearly discount is now derived, not two independently hardcoded numbers: a `MONTHLY_PRICE_CENTS` map plus a `YEARLY_MONTHS = 10` constant, with each plan's `priceYearlyCents` computed as `priceMonthlyCents * YEARLY_MONTHS` — a monthly price change can't quietly desync the "two months free" discount. Extracted the repeated field shape into a local `PlanConfig` type so `PLANS`'s closing `} as const satisfies Record<Plan, PlanConfig>` fits on one line.
- [x] 8.35 Run Now shows a spinning `LoaderCircle` in place of the outline `Play` triangle while `isRunning` (it was already disabled in that state). Verified live: the flag only covers the trigger POST plus the immediate reload, typically well under a second — the actual background Scan keeps running afterward with no client-side indicator beyond History's static "Carl is reading…" row, which only updates on a manual reload. Flagged to the user rather than silently building a longer-lived indicator, since that would need polling and wasn't asked for.
- [x] 8.36 Scan recaps render as markdown instead of raw text. Added `markdown-to-jsx` (~6 kB, zero deps — chosen over react-markdown, whose remark/rehype pipeline is ~10x heavier and its GFM power unnecessary for the bounded heading/bold/list/hr vocabulary these LLM recaps use). New `ui/src/components/ScanNotes.tsx` exports `ScanMarkdown` (a renderer whose `overrides` map every element to the compact card typography and route links through `AnchorLink`) and `ScanNotes` (a collapsible wrapper). Wired into all three recap spots: the topic page's `Carl's Notes` card (collapsible), the scan-detail popover (scrollable), and the homepage topic popover (scrollable). Read more / less clips the *rendered* output to a max height (never slices raw markdown mid-syntax) with a soft fade, and the toggle only appears once the note actually overflows — measured via a ref against the collapsed height, re-measured when the note changes. Verified live: a 2905-char recap renders headings/bold/lists, clips with a fade, and Read more/less expands and collapses; a short recap shows no toggle.
- [x] 8.38 A Claude-style shimmer sweeps across "Carl is reading…" while a scan runs. New `.shimmer-text` utility in `animations.css` (a `--muted-foreground`→`--foreground`→`--muted-foreground` gradient clipped to text, sweeping via a `shimmer-sweep` keyframe; reduced-motion falls back to plain muted text). Applied to History's running-scan stat (extracted `ScanStat` to avoid a nested ternary), and — while scanning — the Run-now trigger is replaced by a bigger shimmering "Carl is reading…" held at the button's `min-h` so the control row never jumps (the in-button `LoaderCircle` spinner is superseded and removed). Verified live: both shimmer spots report `animationName: shimmer-sweep` with transparent text over a gradient, in both themes, and the button swap holds its position.
- [x] 8.39 Read more/less now lives inside the scan-detail and homepage popovers too (swapped their `overflow-y-auto` scroll containers for the collapsible `ScanNotes`; the fade's `from-card` is correct there since `--popover` equals `--card`). The scan-detail popover's label renamed "Carl's scan summary" → "Carl's Notes". `ScanMarkdown` is now internal to `ScanNotes` (un-exported). Verified live: the popover shows the "Carl's Notes" label and a working Read more with rendered markdown headings.
- [x] 8.40 A Scan keeps at most `MAX_KEPT_PER_SCAN = 20` Findings (`worker/review.ts` breaks the review loop once 20 are kept), so a check-in is a digest not a firehose — the tail is left unscored for the next Scan rather than kept. Verified live: a real scan found 41 resources and kept 19 (≤ 20, the cap didn't bind this run but earlier scans on the same topic kept 26 and 33, which would now trim to 20). 20 is a reasonable, easily-tuned ceiling — flagged to the user that 10–15 would feel tighter.
- [x] 8.41 Refinements superseding 8.39/8.40: (a) the popovers went back to scrolling instead of Read more — a new `ScanScroll` (in `ScanNotes.tsx`) renders the recap in a bordered box (`border-border`, 1px) that always scrolls (`overflow-y-scroll`) with an explicit thin webkit scrollbar (`w-1.5`, rounded muted thumb), verified live as a real 8px-wide non-overlay scrollbar; the topic-page `Carl's Notes` card keeps its Read more/less. `ScanMarkdown` stays internal, `ScanScroll` is exported alongside `ScanNotes`. (b) The scan cap changed from "20 kept per scan" to "20 unread per owner": `MAX_UNREAD_FINDINGS = 20`, and `reviewScan` computes `unreadKeepBudget` (20 minus the owner's current unread Findings — Findings with no consumption row) and keeps only that many, short-circuiting to no paid review when the backlog is already full. Total Findings still accrue (read ones stay); only the unread backlog is bounded. Verified live: a topic with 128 unread ran a scan that found 36 Resources and kept 0 (4¢ spent on discovery only, the paid review was skipped). Owner-based since Findings are global but unread is per-user — noted as the reasonable choice pre-auth.
- [x] 8.42 The hero's "Carl stays up. You stay informed." gets one shimmer glint after it animates in (a `.shimmer-once` class: a `--primary`→white→`--primary` gradient clipped to text, swept once via `shimmer-once-sweep` on a 1s delay so it fires just after the hydrate entrance; reduced-motion shows solid accent text). Also dropped its `text-sm` so it sits at the base 16px like the CTA line above it. Verified live: computed `animationName: shimmer-once-sweep`, `iteration-count: 1`, font-size 16px.
- [x] 8.43 Terminology check: kept "shimmer" — it is the idiomatic name for a sweeping-highlight loading/activity effect (popularized by Facebook's Shimmer), over alternatives like "shine" or "gradient sweep". No rename.
- [x] 8.44 Findings are kept and shown by relevance. Display was already ranked (`loadTopicFindings` orders `desc(relevanceScore)`). The keep changed to true top-N: `reviewScan` now runs a two-pass — pass 1 gates every Resource through the free dedupe/relevance stages (`gateResource`), pass 2 fetches and scores every survivor under the spend cap (`scoreCandidate`, no longer writes inline), then it keeps only the highest **relevance-score** survivors up to the unread budget and writes their Findings, dropping the rest (counting as filtered). First tried ranking by the free embedding similarity (cheaper — only scores the top N), but a live scan showed it kept embedding-similar items the model then scored 0, so the ranking key is the model's own relevance score, matching what the UI displays. Cost is the same as the original pipeline (scores every survivor up to the $0.5 budget); the tail beyond budget defers to the next scan. Verified live twice: a fresh topic found 50 Resources and kept the top 20 by score (filtered 30), and a re-run found 48 and kept 20 whose scores were all 1.0 — no 0-scored junk in the kept set, confirming the LLM-score ranking (the embedding version had kept 0-scored items).
- [x] 8.47 Scan `cost` was already admin-only (the api sends `cost: null` to non-admins and the popover renders it only when non-null) — confirmed, no change needed. The recap scroll box (`ScrollBox`, extracted from `ScanScroll` in `ScanNotes.tsx` and switched to `overflow-y-auto` so short content shows no empty track) now also wraps the per-resource "Carl's notes" (`relevanceExplanation`) in `TopicResource`, so a long relevance note stays contained. Hero + README copy: "he has notes for you." → "he has notes.", and "Carl will brew a hot cup of what you missed." → "He'll brew a hot cup of what you just missed." (wrapped in a JS expression for the apostrophe, per jsx-conventions). Footer: dropped `text-xs` on the Attribution and legal rows so they sit at the footer's base `text-sm`, "Go learn from him. Carl would." → "Carl did.", and the current legal page's link (Privacy or Terms) stays underlined via `useLocation` (verified: on /privacy the Privacy link computes `text-decoration: underline`, Terms none).
- [x] 8.48 Fixed the hero glint sticking on "You stay" when the line wraps on a narrow screen. Root cause: the repeating gradient lived in the base `.shimmer-once` style with `both` fill, so a tiled band froze on the text after the sweep, and wrapping shifted where it landed. Fix: the gradient + `color: transparent` now live only inside the keyframes (no fill mode), so before and after the 2s sweep the text is the base solid accent with no `background-image` at all — verified live at 375px that the wrapped two-line text rests at `rgb(217,151,74)` (opaque) with `backgroundImage: none`. Supersedes 8.42/8.46's fill-based timing.
- [x] 8.45 The admin quota lines now read "Unlimited" instead of the bare sentinel. `ADMIN_QUOTA` moved to `shared/plans.ts` (exported) so client and server share it; `api/topicPage.ts` imports it. Extracted `ScanQuotaLine` (topic page) and `TopicQuotaLine` (homepage) that render a loading placeholder, "Unlimited scans" / "Unlimited" when `remaining >= ADMIN_QUOTA` (no pricing link — an admin has nothing to upgrade), else the "N left" pricing link. Verified live: both show "Unlimited …" and "999" is gone from the page.
- [x] 8.46 The hero glint is now a single slow sweep (superseding 8.42's timing): the gradient band starts fully off-screen (`background-position: 250%`) so the hydrate entrance shows plain accent text rather than a first glint, and the sweep is slower and later (`2s ease-in-out 1.3s`), fixing the "twice, fast then slow" the user saw (the fast one was the hydrate blur-clear passing over the still-on-screen band). Verified live: computed `duration 2s`, `delay 1.3s`, `iteration-count 1`.
- [x] 8.37 The topic page polls itself live while a scan runs, so History's status row and the Run-now button follow a scan to completion with no manual reload. Chose polling over SSE: scans are fire-and-forget in-process DB writes with no completion signal to push, the Neon serverless driver doesn't cleanly support LISTEN/NOTIFY, long-lived connections fight serverless, and the `ponytail:` seam becomes Temporal later (which would invalidate any in-process event wiring) — a 3s poll is cheap, self-limiting, and survives that migration unchanged (SSE is the revisit once scans emit Temporal signals). Chose a hook over context since the running state derives entirely from `page.scans`, which already lives in `TopicPage`: `usePollWhileScanning(isScanning, reload)` runs one `setInterval` while `isScanning`, and because History and the button both render from `page`, refreshing it updates both for free. `isScanning` also feeds the button's spinner/disabled state (combined with the optimistic `isRunningScan` for the click gap). Guarded against a crash-orphaned scan (which would otherwise stay `running` forever and poll/disable Run-now permanently) with a 5-minute recency window on `startedAt` — a client stopgap noted in a `ponytail:` comment, the real fix being server-side reaping of stuck scans. Verified live: a real scan triggered from the button flipped the button to a disabled spinner and History to "Carl is reading…", polled while running, then on completion flipped the button back, showed "read N · kept M", refreshed Findings and the rendered Carl's Notes, and stopped polling (0 requests after done); a `setInterval` tracker confirmed exactly one live interval throughout (no leak — earlier rate wobble was just Chrome throttling the interval while the automation tab was backgrounded). Also cleaned one dev scan orphaned in `running` by an earlier mid-scan server restart (one-off `db/fixZombies.ts`, since removed).
- [x] 8.49 The three info popovers (topic, resource, homepage-topic) gained a close ✕ in the top-right corner, and resource rows highlight on hover. New `PopoverCloseButton` in `popover.tsx` wraps radix `PopoverPrimitive.Close`, pinned `absolute top-2 right-2` (with `relative` added to `PopoverContent` so it anchors to the panel), rendered as the first child of each `PopoverContent`; in `Topic.tsx` the info blocks moved into an inner `space-y-3` div so the button sits outside the spaced group and doesn't shift the first block. The `TopicResource` row wrapper gained `hover:bg-accent/60 rounded-lg transition-colors` since the whole row is the click target. Verified live: each popover shows the ✕ in the top-right and dismisses on click (the only DOM that lingers is radix's brief exit-animation Presence), and a hovered row computes a non-transparent `bg-accent/60` background.
- [x] 8.50 The hero tagline re-glints on every route change: `<p key={pathname} className="shimmer-once">` (via `useLocation`) so React remounts it on navigation, replaying the one-shot shimmer with no new CSS. Verified the remount fires — a tagged tagline node is replaced by a fresh node carrying `shimmer-once-sweep` after a client-side nav. The moving glint can't be captured in the automated preview because the preview tab reports `visibilityState: hidden`, which pauses `requestAnimationFrame` and freezes any animation created after the initial load (the initial full-load glint still paints); on a real visible tab the timeline ticks and it sweeps on each route change.
- [x] 8.51 Resource rows: a clearly-visible hover highlight, a grouping indent, and the persona attribution pulled into the hero. The rows sit directly on `--background` with no card behind them, and `--accent` is lighter than that background, so `hover:bg-accent/60` was nearly invisible. Switched to `hover:bg-accent-foreground/20` — a warm brown overlay that darkens the light background and lightens the dark one, adding contrast in both themes — and added `group` so the row wakes up on hover (`group-hover:text-foreground` on the icon, `group-hover:text-foreground` on consumed titles, `group-hover:text-foreground/80` on the metadata). Gave the row `pl-3` and moved the info button from `right-0` to `right-3`, so the highlight carries matching ~0.75rem insets and the content sits indented under its topic. Verified live in both themes (light darkens to `oklab(.50 … / .2)`, dark lightens). Separately, `.shimmer-once` glinted twice because its gradient's `background-repeat` defaulted to `repeat`, so the 400% sweep dragged two tiled bands across; rebuilt it as one non-repeating band (`background-repeat: no-repeat`, `background-size: 250%`) parked off the right edge at rest (reads solid accent) and swept once to `100%` over `2s ease-in-out 1.3s` — verified computed `background-repeat: no-repeat`. Finally, a `SquarePen` button now sits inline right after "…he has notes." in the hero and opens a popover holding the persona attribution (label, the credit paragraph with its links, and a close ✕); `Attribution` was removed from `Footer` so it is not duplicated. Verified live: the popover opens and the ✕ dismisses it, and the footer reads cleanly without it.
- [x] 8.52 Follow-ups on 8.51. The tag pills now align with the resource icons: added `pl-3` to the topic's tag row so tags share the resource rows' left edge (verified both at x=28), leaving the title as the outdented header. Reverted the info pen from `right-3` back to `right-0` — the link's `pr-10` clearance was already enough, so the extra right inset was dropped. `Attribution` became a shared exported component: it lives in `Footer.tsx`, is imported by `Header.tsx`, and renders in both the footer (restored, where it was) and the hero popover, so 8.51's footer removal is undone without duplicating the markup. "Go learn from him. Carl did." moved onto its own line (a second `<p>`), in both places. The hero popover's label changed from "Attribution" to "Inspiration" (the footer still shows the credit with no label). Finally, the hero glint now sweeps left to right instead of right to left: swapped the `shimmer-once-sweep` keyframe endpoints (`from 100%` to `to 0%`) and parked the resting band off the left edge (`background-position: 100%`) — verified the resting position reads `100% 50%`.
- [x] 8.53 Topic-page and homepage alignment polish. The persona popover's label changed from "Inspiration" to "The Real Carl". On the topic page the History `ScanRow` and the shared `ExpanderButton` both gained `pl-3`, so history rows, the findings "+ N more", and the history "+ N older" all line up with the findings rows' icons — verified the findings icon, "+ N more", and history timestamp all sit at x=28. The homepage expander got the same `pl-3` for consistency with its indented findings. Fixed the stepped "+ more" underline: the `link` button variant underlines the whole button on hover, and the larger `text-lg` arrow sits at a lower baseline, so the line stepped between the label and the glyph. The button now carries `hover:no-underline` and the label text is wrapped in a span that underlines on `group-hover`, leaving the arrow outside the underline (verified in the DOM: button `hover:no-underline`, label span `group-hover:underline`, arrow span has no underline class). Finally, the homepage topic title got `pl-0.5 pt-1` so it reads as a slightly inset, top-spaced header above its tags and findings.
- [x] 8.54 Light/dark mode now resolves before first paint and respects the OS setting. A small inline script in `index.html`'s `<head>` runs before render and toggles the `dark` class from `localStorage.theme` when a choice is saved, otherwise from `prefers-color-scheme` — so the page never flashes the wrong theme, and a dark-OS visitor gets dark on the very first load. `useTheme` was simplified to match: it seeds its state from the class the head script already applied (no default-to-light, no post-mount `useEffect`) and writes to localStorage only on an explicit toggle, so it no longer clobbers the system preference on first visit the way the old mount effect did. Verified every path live by emulating the OS scheme and clearing/seeding localStorage: no-saved + system-dark → dark with localStorage untouched, saved "light" + system-dark → light (the explicit choice wins), no-saved + system-light → light, and a toggle persists "dark" and survives reload with the toggle's label and React state matching the DOM.
- [x] 8.55 The `Attribution` component's call to action, "Go learn from him. Carl did.", now centers on its own line while the credit paragraph above it stays left-aligned. Because the component is shared, this centers it in both the footer and the hero popover — verified the CTA computes `text-align: center` and the credit stays `left` in both places.
- [x] 8.56 Lowered the scan-history cap `MAX_SCANS` from 10 to 5 so it matches the findings cap (`MAX_FINDINGS`) — both collapsible sections on the topic page now reveal the same number of rows before their "+ N more / + N older" expander, which keeps the compact history log from dominating the page. Verified live: a topic with 8 scans shows 5 rows plus a "+ 3 older" expander that opens to all 8 and toggles back with "show less".
- [x] 8.57 Moved the one-shot glint from the tagline "Carl stays up. You stay informed." to the hero headline "He already read it. All of it.", keeping the replay on every route change and on initial load. The headline is two-tone (cream "He already read it." + accent "All of it."), so the old text-recoloring `.shimmer-once` would have flattened both to one color. Replaced it with `.shimmer-glint`: the h1 carries `data-text` and a `::after` copy of the headline is laid over the real text (`position: absolute; inset: 0`) and filled with a single non-repeating transparent→white→transparent band that sweeps once, so the glint passes over the letters while the two-tone colors underneath stay intact. The white light travels the whole line but only lights up the accent "All of it." — a white band turns each letter pure white as it passes, which reads as nothing on the near-white cream half, so in effect only the accent glints. That accent-only white glint is the chosen look; a colored band was explored first so the glint would also show on the cream half (gold `#f3c969` read too yellow, then a caramel/crema `#eabd80`→`#d9a866`, going about as dark as still glints), but the plain white on just the accent was what won.
- [x] 8.58 The shared `Attribution` gained an `inlineCta` prop so its call to action renders differently per surface. The footer (`<Attribution inlineCta />`) now flows "Go learn from him. Carl did." inline right after "Clief Notes." in the credit paragraph — left-aligned, no separate line. The hero popover keeps the default (`inlineCta` false): the credit paragraph, then the CTA on its own centered line below. Verified live: the footer Attribution is a single left-aligned paragraph ending "…Clief Notes. Go learn from him. Carl did.", while the hero popover still has two paragraphs (credit left, CTA centered).
- [x] 8.59 The Terms of Service page (`ui/src/pages/TermsPage.tsx`) went from a stub to the full provided legal copy. The 16 numbered sections plus a Contact block render from a markdown body through the app's existing `markdown-to-jsx` (the lib ScanNotes already uses), with legal-page typography overrides — display-font section headings, readable foreground body, disc bullet lists — in a `max-w-3xl` reading column below the title and the effective/last-updated line. `disableAutoLink: true` keeps the bracketed emails and bare domains from turning into stray links, so every `[...]` placeholder ([DATE], [LEGAL NAME / Evan Tsao], [support@carlnotes.com], [dmca@carlnotes.com], [San Mateo County, California]) renders verbatim for filling in before launch. Verified live: the title, 17 headings, the two bullet lists (10 and 8 items), the all-caps disclaimers, and zero links. Privacy page followed in 8.60.
- [x] 8.60 The Privacy Policy page (`ui/src/pages/PrivacyPage.tsx`) went from stub to the full provided copy, built like the terms page — markdown body through `markdown-to-jsx` with legal typography, `disableAutoLink`, in a `max-w-3xl` column — plus two extras it needs: a `strong` override for the bold inline labels, and a table. The providers table renders inside a `ScrollableTable` wrapper (`overflow-x-auto`, `min-w-[34rem]`) so it scrolls rather than cramping on a phone, with `border-separator` row rules on the cells. Its effective/last-updated dates are set to July 30, 2026, and the name is baked into the copy as Evan Shing Tsao; the bracketed placeholders ([privacy@carlnotes.com], [Stripe], [MAILING ADDRESS…]) render verbatim. On the terms page the two [DATE] placeholders became July 30, 2026 and [LEGAL NAME / Evan Tsao] became Evan Shing Tsao (its email and county placeholders stay). Verified live: Privacy shows the title, the filled date, 14 headings, two bullet lists, the 11-row provider table wrapped to scroll, 11 bold labels, and zero stray links; Terms shows the new name and date with its other placeholders intact.
- [x] 8.61 Dropped the age-eligibility paragraph from the terms page's section 3 ("You must be at least 13…"), per request. Section 3 now holds only the account-responsibility paragraph; its heading is still "Eligibility and accounts", so it could be renamed to just "Accounts" if the eligibility content stays out. Verified live: the paragraph is gone and the accounts paragraph remains.
- [x] 8.62 Finalized the legal-page placeholders and made the contact emails clickable. On the mailto-vs-Turnstile question, recommended plain `mailto:` links over gating them behind a Turnstile challenge — role-based legal/support/DMCA addresses should be trivially reachable, a CAPTCHA works against the DMCA safe-harbor intent and adds friction, spam to role addresses is handled by mailbox filtering, and Turnstile fits a form rather than an email reveal. So both pages gained an `a` override routing through `AnchorLink`, and the emails became explicit markdown mailto links (support@ and dmca@ on Terms, privacy@ on Privacy), rendering with `href="mailto:…"` and no visible brackets. Dropped the brackets from the confirmed values too: `[Stripe]` → Stripe in the Privacy table, `[San Mateo County, California]` → plain text in Terms §14. Terms now has zero placeholders; Privacy's only remaining one is `[MAILING ADDRESS, if required by your jurisdiction]`, left bracketed since it is conditional and unfilled. `disableAutoLink` stays on so bare domains like carlnotes.com remain text. Verified live: Terms renders 3 mailto links and no leftover brackets; Privacy renders 3 mailto links, Stripe plain, and only the mailing-address placeholder bracketed.
- [x] 8.63 Dropped the `[MAILING ADDRESS, if required by your jurisdiction]` line from the Privacy Contact section, per request, so the Contact block ends with just the privacy@ mailto link. Both legal pages now carry zero bracketed placeholders. Verified live: no brackets remain anywhere on Privacy and the Contact heading is followed directly by the email link. The h1 is keyed by `pathname` so the glint replays on navigation and the first load. The tagline is back to plain solid accent text, and `.shimmer-once` was removed (no other users). Verified live: the `::after` computes `content` = the data-text, `animation-name: shimmer-glint-sweep`, and `background-clip: text`; the h1 is `position: relative`; the headline renders its two-tone cleanly at rest with the overlay transparent; and the tagline is opaque with no text clip.
- [x] 8.64 Plan tier and pricing finalized, reconciling 8.31/8.33 to what shipped: the third tier renamed `pro` → `premium` across the `plan` enum, `PLANS`, and `db/schema.test.ts`, with migration `0018_faithful_shinobi_shaw.sql` rewriting the enum and updating existing `pro` rows to `premium` — so every `pro` above now reads `premium`. Caps and prices settled in `shared/plans.ts`: premium is 25 topics / 50 daily scans (down from the earlier 50/100) at $29/mo ($290/yr) and plus is $15/mo ($150/yr); the free caps (3 / 5) and plus caps (10 / 20) are unchanged.
